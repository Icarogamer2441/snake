i: int = 0;
range(10).for(i):
    i++;

print(i);

# compiled code:
# i: int = 0
# for i in range(10):
#     i = i + 1

# another example:

mylist: list[int] = [1, 2, 3, 4, 5];
enumerate(mylist).for(idx, val):
    print(f"Index {idx}: {val}");